import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AdvancedsignsofAgeingPage } from './advancedsignsof-ageing.page';

describe('AdvancedsignsofAgeingPage', () => {
  let component: AdvancedsignsofAgeingPage;
  let fixture: ComponentFixture<AdvancedsignsofAgeingPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AdvancedsignsofAgeingPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
