import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TintedglowduoPage } from './tintedglowduo.page';

describe('TintedglowduoPage', () => {
  let component: TintedglowduoPage;
  let fixture: ComponentFixture<TintedglowduoPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(TintedglowduoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
